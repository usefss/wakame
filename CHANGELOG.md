## 0.1.0 - First Release
* yet learning how to catch time with editing code
* this needs to connect to a postgress db on port 5432 for saving actions
* adding event listeners on key press and click and mouse move just i think no need for \
  adding event on scroll maybe later
* next what? need to make a system that get sharjed by a event firing up then after some time\
gets off and for example a boolean that gets false after some minute noting happens
